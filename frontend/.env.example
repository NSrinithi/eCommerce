VITE_APP_NAME=MERN Base
# Relative /api uses the Vite proxy in development and Express in one-host production.
VITE_API_BASE_URL=/api
# Development-only proxy target; not a browser secret.
DEV_API_TARGET=http://127.0.0.1:5000
