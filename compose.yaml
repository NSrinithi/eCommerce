# Optional LOCAL database only. Never expose this unauthenticated DB to the internet.
services:
  mongo:
    image: mongo:8.0
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
